<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BSM322App.HaberDetayPage"
             Title="Haber Detayı">

    <Grid BackgroundColor="{AppThemeBinding Light=White, Dark=#1a1a1a}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Frame Grid.Row="0" BackgroundColor="{AppThemeBinding Light=#f0f0f0, Dark=#2a2a2a}" 
               CornerRadius="0" HasShadow="True" Margin="0,0,0,5">
            <StackLayout Orientation="Horizontal" HorizontalOptions="End">
                <Button Text="Paylaş" 
                        BackgroundColor="#2196F3" 
                        TextColor="White"
                        CornerRadius="15"
                        Padding="15,8"
                        Clicked="OnShareClicked"/>
            </StackLayout>
        </Frame>

        <!-- WebView -->
        <WebView x:Name="HaberWebView" 
                 Grid.Row="1"
                 VerticalOptions="FillAndExpand" 
                 HorizontalOptions="FillAndExpand"/>

        <!-- Loading Indicator -->
        <Grid Grid.Row="1" x:Name="LoadingGrid"
              BackgroundColor="{AppThemeBinding Light=#80FFFFFF, Dark=#80000000}"
              IsVisible="True">
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <ActivityIndicator x:Name="LoadingIndicator" 
                                  IsVisible="True" 
                                  IsRunning="True" 
                                  Color="#2196F3" 
                                  Scale="1.5"/>
                <Label Text="Haber yükleniyor..." 
                       FontSize="16" 
                       TextColor="{AppThemeBinding Light=#2196F3, Dark=#64B5F6}"
                       HorizontalOptions="Center"
                       Margin="0,10,0,0"/>
            </StackLayout>
        </Grid>
    </Grid>

</ContentPage>